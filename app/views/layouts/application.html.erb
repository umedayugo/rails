<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
<body>
  <header class="navbar-inverse">
<div class="container">

  <span>Bookers</span>

 <div id="slidemenu">
  <ul class="nav navbar-nav navbar-right" data-hover="dropdown" data-animations="zoomIn fadeIn fadeInUp fadeIn">
    <% if user_signed_in? %>
      <li class="">
        <%= link_to "Home", user_path(current_user), class:"glyphicon glyphicon-home home" %>
      </li>
      <li class="">
        <%= link_to 'Users', users_path, class:"glyphicon glyphicon-user users" %>
      </li>

      <li class="">
        <%= link_to 'Books', books_path, class:"glyphicon glyphicon-book books" %>
      </li>
      <li class="">
        <!-- ↓以下のコードは、5章で正しいroutingに書き直します。 -->
        <%= link_to "logout", destroy_user_session_path, method: :delete %>

      </li>
    <% else %>
      <li class="">
        <%= link_to "Home", root_path, class:"glyphicon glyphicon-home home" %>
      </li>
      <li class="">
        <%= link_to "About", home_about_path, class:"glyphicon glyphicon-user users" %>
      </li>
      <li class="">
        <%= link_to "sign up", new_user_registration_path, class:"glyphicon glyphicon-book books" %>
      </li>
      <li class="">
        <%= link_to "login", new_user_session_path, class:"glyphicon glyphicon-log-out logout" %>
      </li>
    <% end %>
  </ul>
</div>
</div>
</header>

  <% flash.each do |key, value| %> 
      <%= content_tag(:div, value, class: "#{key}") %> 
  <% end %>
<div class="container">
<%= yield %>
</div>
</body>